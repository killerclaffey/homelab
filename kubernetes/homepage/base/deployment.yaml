apiVersion: apps/v1
kind: Deployment
metadata:
  name: homepage
  namespace: homepage
  labels:
    app: homepage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homepage
  template:
    metadata:
      labels:
        app: homepage
    spec:
      containers:
        - name: homepage
          image: ghcr.io/gethomepage/homepage:latest
          command:
            - /bin/sh
            - -c
            - |
              echo "Initializing writable config directory..."
              cp -r /app/src/skeleton/* /app/config/
              cp /app/config-templates/bookmarks/bookmarks.yaml /app/config/
              cp /app/config-templates/services/services.yaml /app/config/
              cp /app/config-templates/widgets/widgets.yaml /app/config/
              cp /app/config-templates/settings/settings.yaml /app/config/
              mkdir -p /app/config/logs
              echo "Starting Homepage server..."
              exec node server.js
          ports:
            - containerPort: 3000
              name: http
          resources:
            requests:
              cpu: 10m
              memory: 50Mi
            limits:
              cpu: 200m
              memory: 150Mi
          env:
            - name: HOMEPAGE_ALLOWED_HOSTS
              value: homepage.apps.okd.claffey.cloud
          volumeMounts:
            - name: config-dir
              mountPath: /app/config
            - name: config-bookmarks
              mountPath: /app/config-templates/bookmarks
            - name: config-services
              mountPath: /app/config-templates/services
            - name: config-widgets
              mountPath: /app/config-templates/widgets
            - name: config-settings
              mountPath: /app/config-templates/settings
      volumes:
        - name: config-dir
          emptyDir: {}
        - name: config-bookmarks
          configMap:
            name: homepage-bookmarks
        - name: config-services
          configMap:
            name: homepage-services
        - name: config-widgets
          configMap:
            name: homepage-widgets
        - name: config-settings
          configMap:
            name: homepage-settings
